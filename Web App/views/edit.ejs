<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Appointment</title>
    <link href="design.css" rel="stylesheet">
    <style>
body {
    font-family: Arial, sans-serif;
    background-color: #f4f4f4;
    margin: 0;
    padding: 0;
}

form {
    max-width: 400px;
    margin: 20px auto;
    padding: 20px;
    background-color: #fff;
    border-radius: 5px;
    box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
    text-align: center;
}

h3 {
    text-align: center;
}

p {
    text-align: center;
}

label {
    display: block;
    margin-bottom: 8px;
    font-weight: bold;
}

input[type="text"] {
    width: 100%;
    padding: 10px;
    margin-bottom: 10px;
    box-sizing: border-box;
    border: 1px solid #ccc;
    border-radius: 4px;
}

input[type="submit"] {
    width: 100%;
    padding: 12px;
    margin-top: 10px;
    background-color: #4CAF50;
    color: white;
    border: none;
    border-radius: 4px;
    cursor: pointer;
    transition: background-color 0.3s ease;
}

input[type="submit"]:hover {
    background-color: #45a049;
}

select#status {
    text-align: center;
    width: 30%;
    padding: 12px; /* Increase padding to make it taller */
    margin-bottom: 10px;
    box-sizing: border-box;
    border: 1px solid #ccc;
    border-radius: 4px;
}

input[type="datetime-local"] {
    width: 50%;
    padding: 10px;
    margin-bottom: 10px;
    box-sizing: border-box;
    border: 1px solid #ccc;
    border-radius: 4px;
}

    </style>
</head>
<body>
    <form action="/edit/<%= appointment.apptid %>" method="POST">
        <h1>Edit Appointment</h1>

        <h3>Appointment ID:</h3>
        <p><%= appointment.apptid %></p><br>
        
        <label for="patient">Change Patient:</label>
        <input type="text" id="patient" name="patient" value="<%= appointment.patient %>" required><br>
    
        <label for="clinicid">Change Clinic:</label>
        <input type="text" id="clinic" name="clinic" value="<%= appointment.clinic %>" required><br>
    
        <label for="doctorid">Change Doctor:</label>
        <input type="text" id="doctor" name="doctor" value="<%= appointment.doctor %>" required><br>
    
        <label for="type">Change Type:</label>
        <input type="text" id="type" name="type" value="<%= appointment.type %>" required><br>
    
        <label for="time">Change Appointment Schedule:</label>
        <input type="datetime-local" id="time" name="time" value="<%= appointment.time.toISOString().slice(0, 16) %>"><br><br><br>

        <label for="status">Status:</label>
        <select id="status" name="status" required>
            <option value="Complete" <%= appointment.status === 'Complete' ? 'selected' : '' %>>Complete</option>
            <option value="Queued" <%= appointment.status === 'Queued' ? 'selected' : '' %>>Queued</option>
            <option value="Serving" <%= appointment.status === 'Serving' ? 'selected' : '' %>>Serving</option>
        </select><br>

    
        <input type="submit" value="Save">
    </form>    
</body>
</html>
